from django.urls import path
from . import views

urlpatterns = [
    #path('cheff/', views.Cheffs.as_view()),
    #path('cheff/<str:cheff_id>/', views.CheffsDetail.as_view()),
    #path('cheff/<str:cheff_id>/recipes/', views.Recipes.as_view()),
    #path('cheff/<str:cheff_id>/recipes/<str:recipe_id>/', views.RecipeDetail.as_view()),
    #path('cheff/<str:cheff_id>/recipes/<str:recipe_id>/<str:type>', views.RecipeDetail.as_view()),
]
